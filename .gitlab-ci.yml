stages:
  - publish

github-mirror:
  stage: publish
  script:
    - setsid ./ci/github-mirror.sh
  only:
    - /^(master.*)$/
  tags:
    - elasticlifecycle

elastalert-arm:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "arm" "elastalert" "1.0"
  only:
    refs:
      - /^(master.*)$/
    changes:
      - elastalert/*
  tags:
    - buildarm

elasticsearch-arm:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "arm" "elasticsearch" "7.9.1" "1.4"
  only:
    refs:
      - /^(master.*)$/
    changes:
      - elasticsearch/*
  tags:
    - buildarm

kibana-arm:
  stage: publish
  script:
    - setsid ./ci/docker-deliver.sh "arm" "kibana" "7.9.1" "1.3"
  only:
    refs:
      - /^(master.*)$/
    changes:
      - kibana/*
  tags:
    - buildarm
